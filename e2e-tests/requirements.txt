# Android (Appium + UiAutomator2)
Appium-Python-Client>=3.0.0
selenium>=4.0.0

# Linux AT-SPI real cursor (install only on Linux with display)
# PyGObject  -- usually installed via system package (python3-gi)
# pyautogui>=0.9.54

# Windows (UIA via pywinauto) -- install only on Windows
# pywinauto>=0.6.8

# macOS (NSAccessibility via atomacos) -- install only on macOS
# atomacos>=3.0.0
# pyautogui>=0.9.54

# Desktop real cursor (install on Linux/macOS for AT-SPI/NSAccessibility tests)
# pyautogui>=0.9.54

# Common
pytest>=7.0.0
pytest-timeout>=2.0.0
pytest-xdist>=3.0.0
# zajel-headless-client: installed separately via pip install -e packages/headless-client
